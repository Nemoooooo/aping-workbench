# 阿萍的工作台 · 访问说明

> 最后更新：2026-07-28

## 📱 怎样让它「像真正的 App 一样」全屏打开（重点）
应用本身已经按 **PWA 原生 App** 标准做好（无地址栏、有启动闪屏、状态栏适配、可离线）。
但「看起来像不像 App」取决于**你用什么方式打开**：

- ✅ **用 Safari 打开 GitHub Pages 地址 → 分享 → 添加到主屏幕**：从主屏图标进入即为**全屏无地址栏**，
  并显示品牌启动闪屏，跟原生 App 一致。
- ⚠️ **在 Cloud Studio 预览 / 桌面客户端里点开**：它外层是浏览器视图（带地址栏、返回键），
  所以**一定会显示成网页样子**——这不是应用没配好，而是预览窗口本身就是网页壳。
  请改用下面的 GitHub Pages 地址、在 Safari 中「添加到主屏幕」即可获得 App 体验。

> 应用内已内置一条绿色提示条：当你以普通网页形式打开时，会自动提示「分享 → 添加到主屏幕」。
> 点 × 可关闭，关闭后不再重复提示。

## ✅ 方式一：GitHub Pages（推荐 · 用于「添加到主屏幕」）
**https://nemoooooo.github.io/aping-workbench/**

- 已含「AI动态每日简报」「每日新闻联播深度分析」等新模块。
- 部署在 GitHub，**永久有效**；每天 07:00 / 09:00 由沙箱自动重新生成并推送更新。
- 用 **Safari** 打开此地址，再「添加到主屏幕」，即可像 App 一样全屏使用。

## ✅ 方式二：Cloud Studio 沙箱直出（实时预览）
**https://webview.e2b.gz3.sandbox.cloudstudio.club/?x-cs-sandbox-id=e7e82ab25adc4ae3ba0174333880e505&x-cs-sandbox-port=8000**

- 由沙箱直接对外提供，内容与 GitHub Pages 一致，更新即时可见（无需等 Pages 构建）。
- 如提示登录 Cloud Studio，用你自己的账号登录即可。
- 注意：该地址外层带浏览器壳，**会显示成网页**；要全屏 App 体验请走方式一「添加到主屏幕」。

## 📲 iPhone 添加到主屏幕步骤
1. 用 **Safari** 打开上面的 **GitHub Pages** 链接（方式一）。
2. 点底部 **分享按钮**（方框+向上箭头）→ 向上滑 → 点 **「添加到主屏幕」**。
3. 名称已是「阿萍的工作台」，点「添加」。回到桌面点图标，即全屏进入（无地址栏、有闪屏）。

## 🔄 每日自动更新
- 模块3「每日新闻联播深度分析」：每天 07:00（北京时间）重新生成。
- 模块2「AI动态每日简报」：每天 09:00（北京时间）重新生成，含 Markdown 简报 + 信息图。
- 两个地址都会同步更新；进入对应模块时红点消失（表示已读）。

## 🔒 安全提醒
- 对话中曾出现过的 GitHub / Gitee Token 如已不再使用，建议到对应平台删除以免泄露。
