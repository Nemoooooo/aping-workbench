# 阿萍的工作台 · 最终部署方案（2026-08-11 更新）

## ✅ 主用地址（永久不变，推荐）

### **https://nemoooooo.github.io/aping-workbench/**

GitHub Pages 自动部署，源分支 `main`，状态 `built`。沙箱每天自动推送最新内容，地址永远不变。

## 为什么会换成这个

- 之前 GitHub 从沙箱被墙（git TLS 握手失败），内容冻结在 07-31，只能改用临时 Cloudflare 隧道（`*.trycloudflare.com`，每次重启变地址）。
- **2026-08-11 发现 GitHub 已从沙箱恢复可达**（git ls-remote / api.github.com 均正常）。
- 遂将最新代码推回 GitHub Pages，恢复永久地址。手机本来就能直连 github.io，无需登录。

## 次要备份

- Gitee：`https://gitee.com/cui-pingnemo/aping-workbench`（该账号 Pages 服务不可用，仅作代码镜像备份）
- Cloudflare 隧道：`*.trycloudflare.com`（临时，沙箱重启会变，仅应急备用）

## 自动化机制

调度器 `/workspace/scripts/scheduler.py`（supervisor 托管，沙箱恢复自动运行）：
- 每天 07:00 生成「新闻分析」（模块2），09:00 生成「AI简报」（模块3）
- 生成后**双通道推送**：GitHub Pages（主）+ Gitee（备），任一成功即标记完成
- 推送失败会自动每 30s 重试

8000 端口服务 `serve.py`（supervisor 托管，SPA fallback + `/api/memo` 云端备忘录接口），沙箱恢复自动运行。

## 备忘录云端自动保存（模块7）

- 备忘录内容**每次输入都会自动保存到云端**（防抖 600ms），即使「清除网站数据」或重装 PWA 也不会丢失。
- 云端地址由 `config.json` 的 `backend` 字段提供，调度器会在隧道地址变化时自动同步更新并推送。
- 读取策略（双通道冗余）：优先同源相对 `/api/memo`（PWA 经隧道服务端直接托管时可用）；跨域回退到 `config.json` 指定的后端（GitHub Pages 等外部托管场景）。
- 备份落盘在 `/workspace/data/memo.json`，并随每日推送同步到 GitHub/Gitee 仓库。
- ⚠️ 注意：备忘录内容会随公开仓库的 `data/memo.json` 一同对外可读，请勿记录密码等敏感信息。

## 手机端操作

1. 删掉主屏上旧的工作台图标（长按 → 删除）
2. Safari 打开 **https://nemoooooo.github.io/aping-workbench/**
3. 确认模块2（新闻分析）日期、模块3（AI简报）日期是最新的
4. 分享 → 添加到主屏幕 → 命名「阿萍的工作台」

## 验证清单

- 模块8 名称：「充电站」（不是「英语」）
- 模块2 新闻分析：当天或前一天日期
- 模块3 AI简报：当天日期
- 图标正常显示

## 故障排查

- **内容停在旧日期**：说明沙箱被休眠后刚恢复，调度器正在重新生成（等几分钟刷新）；或 GitHub 又被墙（看 /tmp/scheduler.log 是否有 gnutls 报错），此时改用 Cloudflare 隧道备用地址并通知我。
- **打开 404**：删掉旧图标重新添加；确认用的是 GitHub Pages 主地址而非旧隧道地址。
- **备忘录清空缓存后没恢复**：确认打开的是最新版 PWA（删除旧图标重装，让 Service Worker 更新）；若走 GitHub Pages 且 `config.json` 尚未同步到最新后端，可改用当前 Cloudflare 隧道地址打开以立即生效。
- **GitHub 暂时不可达**：沙箱到 GitHub 网络偶发中断时，调度器每 30s 自动重试补齐推送，恢复后自动上线，无需手动干预；期间可先用 Cloudflare 隧道地址使用全部功能。
